package platform;

//this enum shows level of restriction of the code snippet
public enum RestrictionType {
    UNRESTRICTED,
    TIME_RESTRICTED,
    VIEW_RESTRICTED,
    RESTRICTED // restricted by both parameters
}
